<%= form_for(@offer) do |f| %>
  <% if @offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@offer.errors.count, "error") %> prohibited this offer from being saved:</h2>

      <ul>
      <% @offer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_area :message %>
    <br><%= f.label "Items to trade" %><br>
    <%= f.select :line_item_ids, Item.mine.avaliable.collect {|x| [x.title, x.id]}, {}, :multiple => true %>
    <%= f.hidden_field :item_id, :value => @item.id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
